# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 0
  # maxInstances: 100
  # concurrency: 80
  # cpu: 1
  # memoryMiB: 512

# Environment variables and secrets.
env:
  - variable: BACKEND_BASE_URL
    value: https://photo-levelup-agent-backend-4vpb3dvmfq-an.a.run.app
    availability:
      - BUILD
      - RUNTIME
name: Deploy Backend to Cloud Run

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - '.github/workflows/backend-deploy.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_AI_AGENT_HACKATHON_VOL }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy photo-levelup-agent-backend \
            --source ./backend \
            --region asia-northeast1 \
            --project ai-agent-hackathon-vol \
            --allow-unauthenticated \
            --set-env-vars "GOOGLE_CLOUD_PROJECT=ai-agent-hackathon-vol,GOOGLE_CLOUD_LOCATION=us-central1,AGENT_ENGINE_ID=7761957256307933184,BUCKET_NAME=photo-coach,VERTEXAI_LLM=gemini-3-flash-preview,VERTEXAI_IMAGE_MODEL=gemini-3-pro-image-preview,GOOGLE_GENAI_USE_VERTEXAI=TRUE" \
            --quiet

name: Cleanup Artifact Registry Images

on:
  push:
    branches:
      - "**"
  workflow_dispatch: {}

# Disabled to simplify CI
# jobs:
#   cleanup:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       id-token: write
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#
#       - name: Authenticate to Google Cloud
#         uses: google-github-actions/auth@v2
#         with:
#           credentials_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_AI_AGENT_HACKATHON_VOL }}
#
#       - name: Setup gcloud
#         uses: google-github-actions/setup-gcloud@v2
#
#       - name: Cleanup old Artifact Registry images
#         env:
#           PROJECT_ID: ai-agent-hackathon-vol
#           REGION: asia-northeast1
#         run: ./scripts/cleanup_artifact_images.sh
